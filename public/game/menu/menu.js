import { MachineClient } from '../global.js';
import { SetupAuthRegisters } from '../UIClasses/auth.full.js';
/*

    MachineClient.ClientNetPeer.emit('play', {
      ClassUID: '',
      InviteKey: window.location.search.split('invite=')[1] || null
    });
    */

